import styled from 'styled-components';
import { Separator } from '@radix-ui/react-select';

export const SelectSeparatorRoot = styled(Separator)`
  margin: 0.75rem;
  height: 1px;
  background-color: var(--color-border);
`;
